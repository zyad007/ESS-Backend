type CourseType = {
    id?: Number,
    name?: String,
    description?: String,
    no_participants?: Number,
    teacher_id?:Number,
    code?:String,
    category?:String
}

export default CourseType;