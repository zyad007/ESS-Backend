type CourseType = {
    id?: Number,
    name?: String,
    description?: String,
    no_participants?: Number,
    teacher_id?:Number,
    code?:String,
    category?:String,
    img?:String
}

export default CourseType;